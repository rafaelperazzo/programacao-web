# -*- coding: utf-8 -*-

# Definindo função que converte o número inteiro em lista ~ Ex: 1234 em [1,2,3,4]
def lista (q):
    a = []
    while q:
        a.insert(0,q%10)
        q = (q//10)
    return (a)

# Definindo função que converte a lista no número inteiro ~ Ex: [1,2,3,4] em 1234
def voltar (q):
    soma = 0
    i = 0
    j = len(q)-1
    while (i!=len(q)) and (j!=-1):
        soma = soma + q[i] * (10**j)
        i = i + 1
        j = j - 1
    return soma

# E definindo a função que testa se o número é vampiro.
def vampiro (p,q):
    cont = 0
    for i in range (0, len(a), 1):
        p.reverse()
        if (voltar(p) * voltar(q))==n:
            cont = cont + 1
            break
        else:
            q.reverse()
            if (voltar(p) * voltar(q))==n:
                cont = cont + 1
                break                
    if cont==1:
        return True
    else:
        return False

# Definindo a função que usa a primeira metade de uma lista para criar outra
# Ex: Usa [1,2,3,4] para criar [1,2]
def primeirametade(a):
    p = []
    for i in range ( 0, (len(a)/2), 1):
        p.append(a[i])
    return p

# Definindo a função que usa a segunda metade de uma lista para criar outra
# Ex: Usa [1,2,3,4] para criar [3,4]
def segundametade(a):
    q = []
    for i in range ((len(a)-1), ((len(a)/2)-1), -1):
        q.insert(0, a[i])
    return q

# Definindo a função que usa os extremos e o meio de uma lista para criar outra
# Ex: Usa [1,2,3,4] para criar [1,4,2,3]
def meio(a):
    y = []
    for i in range (0, len(a), 3):
        y.append(a[i])
    for i in range (1, 3, 1):
        y.insert(4, a[i])
    return y

# Definindo a função que usa primeiro indice e pula um lista para criar outra
# Ex: Usa [1,2,3,4] para criar [1,3,2,4]
def pula(a):
    z=[]
    for i in range(0, 3, 2):
        z.append(a[i])
    for i in range (1, len(a), 2):
        z.insert(4, a[i])
    return z
        
# Definindo uma função que verifica se o número tem quatro algarismos
def algarismos(a):
    cont = 0
    while (a//10)!=0:
        a = (a//10)
        cont = cont + 1
    if cont==3:
        return True
    else:
        return False

# # Interface do programa
print(' - Números Vampiros -')
print('Bem-vindo! O que deseja?'+
      '\n1 - Definição de um número vampiro.'+
      '\n2 - Verificar se um determinado número natural de quatro algarismos é um número vampiro.'+
      '\n3 - Definir um intervalo de números de quatro algarismos'+
        ' e verificar quantos números vampiros existem nesse intervalo.'+
      '\n4 - Quais são os números vampiros de quatro algarismos.'+'\n5 - Sair.'+
      '\n ~ Este programa é exclusivamente voltado aos números vampiros de quatro algarismos.~')


# Pedir para o usuário escolher uma opção que deve ser entre 1 e 4.
x = int(input(' >> Digite o número da sua escolha: '))
while x>5 or x<=0:
    print('Por favor, inserir uma escolha válida. Escolhas validas(1,2,3,4,5).')
    x = int(input(' >> Digite o número da sua escolha: '))

# Escolha 1
if x==1:
    print ('\nBoa escolha! Segue abaixo a definição de número vampiro:')
    print ('> Em teoria dos números, número vampiro é um número natural v'+
           ', com número par de dígitos n, ou seja, sua divisão por dois '+
           'é um número natural, que são divididos ao meio, originando x e y.'+
           ' Esses números x e y são formados pelos algarismos do número e podem'+
           ' ser trocados de ordem. Em números vampiros verdadeiros, é possível'+
           ' realizar a multiplicação de x por y e obter novamente o número inicial.')

# Escolha 2
elif x==2:
    print('\nBoa escolha!')
    n = int(input(' >> Digite um número de quatro algarismos para ser testado: '))
    while algarismos(n)==False or n==0:
            print('Por favor, inserir apenas números de 4 digitos. Obrigado!')
            n = int(input(' >>Digite o número a ser testado "n": '))
    a = lista(n)
    p = primeirametade(a)
    q = segundametade(a)
    y = primeirametade(meio(a))
    z = segundametade(meio(a))
    ay = primeirametade(pula(a))
    az = segundametade(pula(a))
    if vampiro(p,q):
        print('O número %s é vampiro, pois,'%n+'%s'%voltar(p)+'*'
              +'%s'%voltar(q)+'='+'%s.'%(voltar(p)*voltar(q)))
    elif vampiro(y,z):
        print('O número %s é vampiro, pois,'%n+'%s'%voltar(y)+'*'
              +'%s'%voltar(z)+'='+'%s.'%(voltar(y)*voltar(z)))
    elif vampiro(ay,az):
        print('O número %s é vampiro, pois,'%n+'%s'%voltar(ay)+'*'
              +'%s'%voltar(az)+'='+'%s.'%(voltar(ay)*voltar(az)))
    else:
        print('O número %d não é vampiro.'%n)

# Escolha 3
elif x==3:
    print('\nBoa escolha!')
    i = int(input(' >> Digite o inicio do intervalo: '))
    while algarismos(i)==False or i==0:
            print('Por favor, inserir apenas números de 4 digitos. Obrigado!')
            i = int(input(' >>Digite o inicio do intervalo: '))
    f = int(input(' >> Digite o final do intervalo: '))
    while algarismos(f)==False or f==0:
            print('Por favor, inserir apenas números de 4 digitos. Obrigado!')
            f = int(input(' >>Digite o final do intervalo: '))
    ii = i
    cont = 0
    for i in range(i,f,1):
        n = i
        a = lista(i)
        p = primeirametade(a)
        q = segundametade(a)
        y = primeirametade(meio(a))
        z = segundametade(meio(a))
        ay = primeirametade(pula(a))
        az = segundametade(pula(a))
        if vampiro(p,q):
            cont = cont + 1
        elif vampiro(y,z):
            cont = cont + 1
        elif vampiro(ay,az):
            cont = cont + 1
    print('Existem %s'%cont+' números vampiros no intervalo entre %s'%ii+' e %s.'%f)

# Escolha 4
elif x==4:
    cont=0
    for i in range(1000,9999,1):
        n = i
        a = lista(i)
        p = primeirametade(a)
        q = segundametade(a)
        y = primeirametade(meio(a))
        z = segundametade(meio(a))
        ay = primeirametade(pula(a))
        az = segundametade(pula(a))
        if vampiro(p,q):
            cont = cont + 1
            print('%s.'%(voltar(p)*voltar(q)))
        elif vampiro(y,z):
            cont = cont + 1
            print('%s.'%(voltar(y)*voltar(z)))
        elif vampiro(ay,az):
            cont = cont + 1
            print('%s.'%(voltar(ay)*voltar(az)))
    print('Ao todo são %s'%cont+' números vampiros de quatro algarismos.')

# Escolha 5 
elif x==5:
    exit()
